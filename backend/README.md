# Kids Story Generator Backend

## Setup Instructions

1. Create virtual environment
2. Install dependencies
3. Run the Flask server

## API Endpoints

- POST `/api/generate-story` - Generate a personalized story
- GET `/api/health` - Health check endpoint